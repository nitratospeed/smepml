import { PredictDiagnosticoDto } from './predict-diagnostico-dto';

describe('PredictDiagnosticoDto', () => {
  it('should create an instance', () => {
    expect(new PredictDiagnosticoDto()).toBeTruthy();
  });
});
